export function UnitsOfStorage() {
    return <>
    <h1>
        Measurements of Computer Storage
    </h1>
    <p>
        Bits, Bytes, Kilobytes, and Megabytes are measurement units for storage in computing. Bytes, kilobytes and megabytes are scaled versions of the individual digits of computing (bits).
    </p>
    <p>
        A byte is 8 bits, a kilobyte is 1000 bytes (Not to be confused with Kibibyte (KiB) which uses 1024 bytes), and a megabyte is 1000 kilobytes.
    </p>
    <h1></h1>
    <p>
        Mega comes from the prefix 'Mega' to denote 1 million.
    </p>
    <p>
        Kilobyte comes from the prefix 'Kilo' to denote 1 thousand.
    </p>
    <p>
        A byte is 8 bits.
    </p>
    <p>
        A bit is one bit.
    </p>
    <p>
        These terms came from the metric system (International System of Units - SI), which derived them from Greek and Latin roots.
    </p>
    <p>
        The prefixes are also used in science fields such as physics, data science.
    </p>
    <p>
        Because computers operate on base-2, early computer scientists found it more convenient to work with binary rather than base-10.
    </p>
    <p>
        1 Kilobyte (KB) is 2^10 bytes = 1024 bytes. The kilo prefix is normally 1000 in other fields, but is 1024 here — which is confusing.
    </p>
    <p>
        1 Megabyte (MB) isi 2^20 bytes = 1048576 bytes. The prefix meant million in other fields, but is applied as 1,048,576 here — which is also confusing.
    </p>
    <p>
        Later, the International Electrotechnical Commission decided to solve this confusion by introducing new, explicit prefixes for these binary prefixes in 1999.
    </p>
    <p>
        They transformed the MB equaling 2^20 into MiB (mega + binary) (Mebi byte).
        Then, they turned 1,000,000 back into Megabyte.
    </p>
    </>;
}